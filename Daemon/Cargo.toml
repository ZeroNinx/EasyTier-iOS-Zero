[package]
name = "easytierd"
version = "0.1.20"
edition = "2021"

[dependencies]
easytier-ios = { path = "../Core" }
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
